<UserControl x:Class="AccountManager.Settings.GeneralSettings"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AccountManager.Settings"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Medium"
             TextElement.FontSize="14"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="200"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>
            <GroupBox Header="Settings" Margin="16">
                <StackPanel Orientation="Vertical">
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                Grid.Column="0" Grid.Row="4"
                                materialDesign:ButtonProgressAssist.Value="-1"
                                materialDesign:ButtonProgressAssist.IsIndicatorVisible="{Binding ShowWisaClassGroupSyncButtonIndicator.Value}"
                                materialDesign:ButtonProgressAssist.IsIndeterminate="True"
                                Content="Export Settings"
                            x:Name="exportButton"
                                Click="ExportButton_Click"
                                Margin="8" />
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                Grid.Column="0" Grid.Row="4"
                                materialDesign:ButtonProgressAssist.Value="-1"
                                materialDesign:ButtonProgressAssist.IsIndicatorVisible="{Binding ShowWisaClassGroupSyncButtonIndicator.Value}"
                                materialDesign:ButtonProgressAssist.IsIndeterminate="True"
                                Content="Import Settings"
                            x:Name="importButton"
                                Click="ImportButton_Click"
                                Margin="8" />
                </StackPanel>
            </GroupBox>

            <GroupBox Grid.Column="1" Margin="16" Header="Preferences">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Margin="8" VerticalAlignment="Top">
                        <ToggleButton IsChecked="{Binding Path=Data.ShowDebugInterface, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignSwitchToggleButton}" ToolTip="Default ToggleButton Style" />
                        <Label Content="Show Advanced Interface"/>
                    </StackPanel>
                </Grid>
                
            </GroupBox>
        </Grid>
        
    </Grid>
</UserControl>
